# Functions and Scope

The moment a colony has more than one creep role, you need functions: reusable behavior you can call by name instead of copy-pasting logic. This lesson covers writing them and understanding what they can and can't see.

This lesson is coming soon. Planned outline:

- Declaring functions, arrow functions, and when to reach for each
- Parameters, return values, and functions that call other functions
- Scope: what a variable inside a function can see, and what it can't
- Turning a repeated block of colony logic into a named, reusable function

Want early access or have a topic you want covered? Ask in Discord.
