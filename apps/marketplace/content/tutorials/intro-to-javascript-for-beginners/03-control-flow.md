# Conditionals and Loops

A colony's logic is mostly branches and repetition: if the creep is full, deliver — otherwise, harvest; for every creep in memory, decide what it should do next. This lesson covers the two building blocks behind almost every line of colony code.

This lesson is coming soon. Planned outline:

- `if` / `else if` / `else`, and reading conditions without tripping over them
- `for` and `for...in` loops, including looping over `Game.creeps` and `Memory.creeps`
- Early exits with `return` and `continue`
- Common mistakes: infinite loops, off-by-one errors, and mutating a list while looping over it

Want early access or have a topic you want covered? Ask in Discord.
