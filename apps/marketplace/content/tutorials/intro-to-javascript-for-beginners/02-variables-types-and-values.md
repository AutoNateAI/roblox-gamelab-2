# Variables, Types, and Values

Every decision a colony makes comes down to values stored somewhere: how much energy is in a room, whether a creep is full, what a spawn is named. This lesson covers how JavaScript holds onto those values.

This lesson is coming soon. Planned outline:

- Declaring values with `const` and `let`, and why Screeps code avoids `var`
- The core types: strings, numbers, booleans, `undefined`, and `null`
- Arrays and objects as containers for grouped values
- Naming values so a future reader (including future you) can tell what they hold

Want early access or have a topic you want covered? Ask in Discord.
